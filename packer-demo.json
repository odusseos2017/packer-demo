{
  "variables": {
    "aws_access_key": "",
    "aws_secret_key": ""
  },
  "builders": [{
    "type": "amazon-ebs",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "region": "us-east-1",
    "source_ami": "ami-0e4d932065378fd3d",
    "subnet_id": "subnet-0ffab7b4c17d466c6",
    "instance_type": "t2.micro",
    "ssh_username": "ubuntu",
    "ami_name": "packer-nodejs-demo {{timestamp}}"
  }],
  "provisioners": [
  {
    "type": "file",
    "source": "app",
    "destination": "/tmp/app"
  },
  {
    "type": "shell",
    "scripts": [  "scripts/deploy.sh"   ],
    "execute_command": "{{ .Vars }} sudo -E sh '{{ .Path }}'",
    "pause_before": "10s"
  }]
}
